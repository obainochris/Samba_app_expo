import { Feather, Ionicons } from '@expo/vector-icons';
import { useLocalSearchParams, useRouter } from 'expo-router';
import React, { useState } from 'react';
import { Alert, Image, Pressable, ScrollView, Text, View } from 'react-native';
import * as Haptics from 'expo-haptics';
import { useSafeAreaInsets } from 'react-native-safe-area-context';
import { getProvider, useBookings } from '@/context/BookingContext';
import { ServiceMode } from '@/data/providers';

const dates = ['Today', 'Sat, 5 Oct', 'Sun, 6 Oct', 'Mon, 7 Oct'];
const times = ['10:00 AM', '12:30 PM', '3:00 PM', '5:30 PM'];

export default function ProviderDetailScreen() {
  const { id } = useLocalSearchParams<{ id: string }>();
  const provider = getProvider(id ?? '');
  const router = useRouter();
  const insets = useSafeAreaInsets();
  const { addBooking } = useBookings();
  const [mode, setMode] = useState<ServiceMode>('home');
  const [selectedService, setSelectedService] = useState(0);
  const [selectedDate, setSelectedDate] = useState(dates[0]);
  const [selectedTime, setSelectedTime] = useState(times[0]);

  if (!provider) return <View className="flex-1 items-center justify-center bg-paper"><Text className="text-ink">Provider not found</Text></View>;
  const service = provider.services[selectedService];
  const book = async () => {
    await Haptics.notificationAsync(Haptics.NotificationFeedbackType.Success);
    await addBooking({ providerId: provider.id, providerName: provider.name, serviceName: service.name, date: selectedDate, time: selectedTime, mode, price: service.price, location: provider.location });
    Alert.alert('Booking confirmed', 'Your ' + service.name + ' appointment with ' + provider.firstName + ' is saved.', [{ text: 'View bookings', onPress: () => router.replace('/bookings') }]);
  };
  return <View className="flex-1 bg-paper"><ScrollView showsVerticalScrollIndicator={false} contentContainerStyle={{ paddingBottom: 38 }}><View className="relative"><Image source={provider.image} className="h-[330px] w-full" resizeMode="cover" /><View className="absolute left-5 flex-row" style={{ top: insets.top + 12 }}><Pressable onPress={() => router.back()} className="h-10 w-10 items-center justify-center rounded-full bg-white/90 active:opacity-70"><Feather name="arrow-left" size={19} color="#27221F" /></Pressable></View><Pressable className="absolute right-5 h-10 w-10 items-center justify-center rounded-full bg-white/90 active:opacity-70" style={{ top: insets.top + 12 }}><Feather name="heart" size={18} color="#27221F" /></Pressable></View><View className="-mt-7 rounded-t-[30px] bg-paper px-5 pt-6"><View className="flex-row items-start"><View className="flex-1"><View className="flex-row items-center"><Text className="text-[27px] font-bold tracking-[-0.7px] text-ink">{provider.name}</Text><Ionicons name="checkmark-circle" size={19} color="#D86D55" style={{ marginLeft: 7 }} /></View><Text className="mt-1 text-[14px] text-smoke">{provider.specialty}</Text></View><View className="items-end"><View className="flex-row items-center"><Ionicons name="star" size={14} color="#BC8A4A" /><Text className="ml-1 text-[14px] font-bold text-ink">{provider.rating}</Text></View><Text className="mt-1 text-[11px] text-smoke">{provider.reviews} reviews</Text></View></View><View className="mt-5 flex-row items-center"><View className="flex-row items-center rounded-full bg-white px-3 py-2"><Feather name="map-pin" size={13} color="#D86D55" /><Text className="ml-1.5 text-[12px] font-semibold text-ink">{provider.location}</Text></View><Text className="ml-2 text-[12px] text-smoke">{provider.distance}</Text></View><Text className="mt-6 text-[14px] leading-6 text-smoke">{provider.bio}</Text><Pressable onPress={() => router.push({ pathname: '/messages/[providerId]', params: { providerId: provider.id } })} className="mt-5 flex-row items-center justify-center rounded-full border border-banner bg-white py-3.5 active:opacity-75"><Feather name="message-circle" size={16} color="#2D6CDF" /><Text className="ml-2 text-[13px] font-bold text-banner">Message {provider.firstName}</Text></Pressable><Text className="mt-7 text-[19px] font-bold text-ink">Choose a service</Text><ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={{ paddingTop: 14 }}>{provider.services.map((item, index) => <Pressable key={item.name} onPress={() => setSelectedService(index)} className={'mr-3 w-[156px] rounded-[19px] border p-4 ' + (selectedService === index ? 'border-terracotta bg-blush' : 'border-line bg-white')}><Text className="text-[14px] font-bold text-ink">{item.name}</Text><Text className="mt-2 text-[11px] text-smoke">{item.duration}</Text><Text className="mt-3 text-[14px] font-bold text-terracotta">{item.price}</Text></Pressable>)}</ScrollView><Text className="mt-7 text-[19px] font-bold text-ink">Where would you like it?</Text><View className="mt-3 flex-row rounded-[17px] bg-cream p-1"><Pressable onPress={() => setMode('home')} className={'flex-1 rounded-[14px] py-3.5 ' + (mode === 'home' ? 'bg-white' : '')}><View className="items-center"><Feather name="home" size={16} color={mode === 'home' ? '#D86D55' : '#8B8581'} /><Text className={'mt-1 text-[12px] font-semibold ' + (mode === 'home' ? 'text-ink' : 'text-smoke')}>My home</Text></View></Pressable><Pressable onPress={() => setMode('shop')} className={'flex-1 rounded-[14px] py-3.5 ' + (mode === 'shop' ? 'bg-white' : '')}><View className="items-center"><Feather name="map-pin" size={16} color={mode === 'shop' ? '#D86D55' : '#8B8581'} /><Text className={'mt-1 text-[12px] font-semibold ' + (mode === 'shop' ? 'text-ink' : 'text-smoke')}>Their place</Text></View></Pressable></View><Text className="mt-7 text-[19px] font-bold text-ink">Pick a time</Text><ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={{ paddingTop: 14 }}>{dates.map((date) => <Pressable key={date} onPress={() => setSelectedDate(date)} className={'mr-2 rounded-full px-4 py-2.5 ' + (selectedDate === date ? 'bg-ink' : 'bg-white')}><Text className={'text-[12px] font-semibold ' + (selectedDate === date ? 'text-white' : 'text-ink')}>{date}</Text></Pressable>)}</ScrollView><ScrollView horizontal showsHorizontalScrollIndicator={false} contentContainerStyle={{ paddingTop: 10 }}>{times.map((time) => <Pressable key={time} onPress={() => setSelectedTime(time)} className={'mr-2 rounded-full border px-4 py-2.5 ' + (selectedTime === time ? 'border-terracotta bg-blush' : 'border-line bg-white')}><Text className="text-[12px] font-semibold text-ink">{time}</Text></Pressable>)}</ScrollView><Pressable onPress={book} className="mt-8 flex-row items-center justify-center rounded-full bg-terracotta py-4 active:opacity-80"><Text className="text-[14px] font-bold text-white">Confirm booking</Text><Feather name="arrow-up-right" size={17} color="#FFFFFF" style={{ marginLeft: 8 }} /></Pressable></View></ScrollView></View>;
}